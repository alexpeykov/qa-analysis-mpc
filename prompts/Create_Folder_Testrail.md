Create a new folder in TestRail Nova Tribe suite with the following details:
- Folder name: Test case folder
- Description: https://jira.paysera.net/browse/CORE-3820

Use the saved configuration in testrail-config.json (projectId: 2, suiteId: 14).

**Note**: The create_testrail_section tool now requires both `project_id` and `suite_id` parameters to be explicitly provided.
