Please take the content of the MD file that you've just generated and add it into a new html report. 
The HTML must have
-All information that the md contains
-All sections
-Headers
-Dinamic table of content in the beggining with hyperlynks leading via anchor tags to the given section
-Links to the jira ticket, merge request,blockers(if any),linked tickets(if any) and documentation (if any). The links should be embeded in clickable text, not just dropped as a link